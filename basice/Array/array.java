public class array {
    public static void main(String[] args) {
        // to declare array size
        // int[] array = new int[3];
        byte s = 4 + 9;
        System.out.println(s);
        // int[] g={ };
        int[] array = { 12, 234, 14 };
        System.out.println(array[0]);
        // we do not need to use () in array becase we do not need to call the function
        // as String in array its array property
        System.out.println(array.length);
    }
}
